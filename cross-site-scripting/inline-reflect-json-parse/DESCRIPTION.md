The page tries to look safer by packing your message into JSON and parsing it in the browser.

The wrapper changed, but the victim's authenticated [cookie](https://developer.mozilla.org/en-US/docs/Web/API/Document/cookie) is still there to be taken.
