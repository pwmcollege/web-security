Your message now passes through an inline JavaScript snippet before it ever reaches the page.

The browser is still doing exactly what it is told, and the victim's authenticated [cookie](https://developer.mozilla.org/en-US/docs/Web/API/Document/cookie) is still there to be taken.
