Your message is now tucked into a JavaScript object before the page reads it back out.

The browser still trusts that structure, and the victim's authenticated [cookie](https://developer.mozilla.org/en-US/docs/Web/API/Document/cookie) is still there to be taken.
