***pwnpost™*** no longer supports admin posting due to rampant flag disclosure.

The flag is still saved as one of admin's drafts. The feed itself does not show it anymore, but admin still reads the feed, and your post is on the page when they do.
